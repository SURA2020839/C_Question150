// Online C compiler to run C program online
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <math.h>
int main() {
int brr[5],i,j,c;
char arr[]="124";
 char *str=arr;
for(i=0;i<strlen(arr);i++)
{
    printf("%c",*(str+i));
    brr[i]= ;
}
for(j=0;j<5;j++)
{
    printf("\n%d for %d ",brr[j],j);
    c= c+brr[i]*pow(10,(strlen(arr)-i) );
}

printf("%d ", c);
    

    return 0;
}